@(versions)
> v4.0.0 is our first release
@(security)
> the data object has token, user, and (or) additional info depeding on the request/event
> user object is only for display purposes. it goes and comes back with every request to
 get updated and update the display. also it carries user selections, like room, amount, move, etc
> all server activities use the token (which only contains an id at the time of this writing) 
except for move event which uses gameId and opponentId, these can be put i a token when ever 
 game is approved

 @(testing)
 > change to SANDBOXPAYPALCLIENTID and SANDBOXPAYPALCLIENTSECRET
 > change to testLogs collection
 > change success and cancel pay ur to localhost
 